\documentclass[Ingles,Final]{thesis}

% Add your bibliography file
\addbibresource{references.bib}

\usepackage{subcaption}

% Prevent infinite page-break retry loops caused by overfull boxes
% (libertinust1math + on-the-fly font loading can trigger retries otherwise)
\emergencystretch=3em

\begin{document}

% Author information
\autor{Felipe Augusto Oliveira dos Santos}
\title{An Adaptive Defense System for IoT Networks Using Proactive Attack Prediction and Deep Reinforcement Learning}

% Advisor information
\orientador{Prof. Dr. Denis Fantinato}

% Type of document
\qualificacaoMestrado
\areaConcentracao{Engenharia de Computação}

% Defense date
\datadadefesa{16}{7}{2025}

% Evaluators
%\avaliadorA{Prof. Dr. Denis Fantinato}{FEEC/UNICAMP}
%\avaliadorB{Prof. Dr. Eric Rohmer}{FEEC/UNICAMP}
%\avaliadorC{Prof. Dr. João Kleinschmidt}{UFABC}

% Start document
\paginasiniciais

% Abstract
\begin{abstract}
The exponential proliferation of the Internet of Things (IoT) has produced a vast network of interconnected devices --- the number of worldwide connections is forecast to more than double from 19.8~billion in 2025 to over 40~billion by 2034 \cite{Statista2025} --- while the global economic impact of cyber threats is projected to reach USD 12.2~trillion annually by 2031 \cite{Morgan2025}. The computational and memory constraints of many IoT devices render traditional, resource-intensive security solutions ineffective, motivating intelligent and adaptive defense mechanisms capable of operating autonomously. This dissertation addresses that need by designing, implementing, and rigorously evaluating an adaptive defense framework for IoT networks that combines proactive attack prediction with Deep Reinforcement Learning (DRL).

The framework is organised as a closed-loop pipeline grounded in the CICIoT2023 dataset \cite{Neto2023}, which captures traffic from 105 real IoT devices across 33 attack types. The dataset is projected onto a five-stage Cyber Kill Chain (\textsc{benign}, \textsc{recon}, \textsc{access}, \textsc{maneuver}, \textsc{impact}), an LSTM-based stage detector and an LSTM red-team episode generator are trained on the resulting stage-labelled splits, and a custom Gymnasium environment exposes a five-action defender (\textsc{observe}, \textsc{log}, \textsc{throttle}, \textsc{block}, \textsc{isolate}) under a stage-action proportionality reward. Three DRL algorithms --- the value-based Deep Q-Network (DQN) and the policy-gradient methods Proximal Policy Optimization (PPO) and Advantage Actor-Critic (A2C) --- are trained over ten seeds and benchmarked on a held-out test split against four reference policies: random, always-\textsc{observe}, always-\textsc{block}, and a supervised RandomForest stage classifier composed with the kill-chain recommended-action mapping (RF-Acting). A rule-based oracle that observes the true attack stage is reported as an upper bound on the value of perfect stage detection.

The trained agents capture \textbf{81\,\%} of the oracle ceiling without observing the true stage (best deployable agent: PPO, mean episodic reward $+1334.5$, 95\,\% bootstrap CI $[1317.3, 1352.3]$, against the oracle's $+1647.6$ on the held-out \texttt{test\_balanced} split). The trained DRL trio dominates every non-RL trivial baseline (random $+411$, always-\textsc{block} $+530$, always-\textsc{observe} $-421$) with non-overlapping bootstrap confidence intervals. Among deployable policies, the comparison is a latency--reward trade-off: RF-Acting achieves higher reward ($+1486$) at an inference cost of 13.85\,ms per decision, while trained PPO achieves $+1334.5$ at 0.098\,ms --- a \textbf{141$\times$ latency advantage}. A reward-component ablation identifies a structural environment change (\texttt{impact\_is\_terminal=False}) that lifts mean reward to $+1542$ and the mitigated-impact rate from $0.153$ to $0.900$ (a $5.9\times$ improvement), closing 71\,\% of the residual gap to the oracle ceiling. On a held-out out-of-distribution attack class (\texttt{VulnerabilityScan}, on which the supervised stage detector has recall $0.001$), the trained policy is \emph{robust to} but not \emph{better at} the OOD class --- PPO's mean OOD reward lies within seed-noise of its in-distribution mean. Every reported number is reproducible from the per-phase \texttt{manifest.json} hash chain and verified end-to-end by the \texttt{reproducibility\_smoke} harness. The contributions are a kill-chain-aware adaptive defense framework grounded on a contemporary IoT dataset, a comparative DRL benchmark with statistically separated baselines, and an audit-first reproducibility protocol.

\keywords{IoT security, Deep Reinforcement Learning, Cyber Kill Chain, Attack Stage Prediction, LSTM, DQN, PPO, A2C, CICIoT2023, Reproducibility}
\end{abstract}


% List of acronyms
\printnomenclature[3cm]

% Table of contents
\tableofcontents

% Reset page counter and start main content with page numbering
\cleardoublepage
\setcounter{page}{1}

% Start main content with page numbering
\fimdaspaginasiniciais

% Include your sections
\input{introduction}
\input{background}
\input{methodology}
\input{results}
\input{conclusions}

% References
\newpage
\begin{singlespacing}
\setlength\bibitemsep{10pt}
\printbibliography[heading=bibintoc, title={References}]
\end{singlespacing}

% Appendices
\newpage
\appendix
\input{appendices}

\end{document}
