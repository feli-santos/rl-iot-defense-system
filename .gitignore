# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE and Editors
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Testing and Coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
*.py,cover
.hypothesis/

# Jupyter Notebooks
.ipynb_checkpoints

# PyTorch Models
*.pth
*.pt
*.ckpt

# MLflow Experiment Tracking (top-level only)
/mlruns/
/mlartifacts/

# Training Artifacts (top-level only — do NOT match docs/results, etc.)
/artifacts/
/runs/
/checkpoints/
/models/

# Results and Logs (top-level only — do NOT match docs/results)
/results/
/logs/
*.log
/tensorboard/

# Local archive bundles (intentional, never committed)
/.archive/

# Dataset Files (top-level only)
/data/raw/
/data/processed/
/data/temp/
*.csv
*.parquet
*.h5
*.hdf5

# Preprocessing Artifacts
preprocessing_artifacts.pkl
*.scaler
*.encoder

# Benchmarking Results
benchmark_results/
benchmark_analysis/
benchmark_plots/

# Memory Bank (project knowledge base for AI)
memory-bank/

# Configuration Overrides (keep main config.yml)
config_*.yml
local_config.yml
experiment_config.yml

# Temporary Files
*.tmp
*.temp
.cache/
temp/

# Environment Variables
.env.local
.env.development
.env.test
.env.production

# Documentation Build
docs/_build/
docs/build/

# Reference PDFs (use docs/papers/REFERENCES.md with DOIs instead)
docs/papers/*.pdf

# Profiling
*.prof
*.lprof

# Distribution
build/
dist/
*.egg-info/

# LaTeX build artefacts (tex/ compile output; principal.pdf / tese.pdf are the
# committed final outputs and remain tracked separately).
tex/*.aux
tex/*.log
tex/*.bbl
tex/*.bcf
tex/*.blg
tex/*.brf
tex/*.fdb_latexmk
tex/*.fls
tex/*.out
tex/*.toc
tex/*.lof
tex/*.lot
tex/*.idx
tex/*.ilg
tex/*.ind
tex/*.glo
tex/*.gls
tex/*.glg
tex/*.nlo
tex/*.nls
tex/*.run.xml
tex/*.synctex.gz
tex/missfont.log
tex/texput.log
# Working directory for build artifacts moved aside by tex/build.sh
tex/_legacy/

# Auto-generated LaTeX fragments from canonical JSONs (re-runnable)
tex/generated/
