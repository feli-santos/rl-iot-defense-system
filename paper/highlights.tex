%% Highlights file (Elsevier requires 3-5 bullets, each <= 85 characters).
%% Character counts (excluding leading "\item ") are given in comments.
\begin{itemize}
\item Partially observable, kill-chain-aware IoT defense on real CICIoT2023 flows % 78
\item Windowed RL ties a memoryless classifier at zero aliasing, then overtakes it % 82
\item Best RL agent outperforms the classifier under sparse and shaped rewards % 77
\item RL prevents more attacks on every one of ten held-out attack classes % 73
\item On-policy agents train stably; advantage is reliability, not peak return % 78
\end{itemize}
