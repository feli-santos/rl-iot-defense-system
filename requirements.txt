# Runtime dependencies for rl-iot-defense-system (Python 3.9, CPU-first).
# Pin to known-good versions; bump deliberately via PR so hash-chain
# reproducibility (docs/results/ manifests) stays intact.
#
# torch is CPU-only in this project. On Linux, prefer the CPU wheel:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu

numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
torch==2.8.0
stable-baselines3==2.7.0
gymnasium==1.1.1
matplotlib==3.9.4
pyyaml==6.0.3
joblib==1.5.2
