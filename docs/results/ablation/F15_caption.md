**F15 — OOD-class robustness.** Prevention rate for each benchmark policy on the ten held-out zero-day classes under the single-stage feature-injection protocol: held-out rows replace only the true kill-chain stage for that class, while other stages draw in-distribution rows. Across every class the best windowed RL agent (A2C at the headline aliasing rate; PPO and DQN shown for comparison) prevents a substantially larger fraction of attacks than the detector-coupled RF-Acting baseline; RF recall spans 0.20--0.998, including `VulnerabilityScan` at 0.224.
